# 의존성 (재설치 가능, 올리지 않음)
node_modules/
poc/node_modules/

# 비밀정보 — 절대 커밋 금지 (인스타 토큰, API 키 등)
.env
.env.*
!.env.example
*.local
secrets/
**/secrets/

# Next.js / 빌드 산출물
.next/
out/
build/
.vercel/
*.tsbuildinfo
next-env.d.ts

# Supabase 로컬 CLI
supabase/.branches/
supabase/.temp/

# OS / 에디터
.DS_Store
Thumbs.db
.vscode/
.idea/

# 로그
*.log
npm-debug.log*

# 대용량 바이너리 (다운로드로 재취득 가능, 올리지 않음)
poc/cloudflared
